<Window x:Class="TeronWoWLauncher.Dialogs.LocalAddonsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Local Addons Found" Height="440" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="#1B1B1F">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#C8C8D0"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#DADADD"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>
        <!-- The app-wide Button style (App.xaml) covers this dialog too — no local override here. -->
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Possible duplicate addons" FontSize="16" FontWeight="Bold"
                   Foreground="#E6C067" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1"
                   Text="These folders in Interface\AddOns share a name with an addon you already track, installed under a different folder. Check any you'd still like to track as a separate entry."
                   TextWrapping="Wrap" Foreground="#8A8A92" FontSize="11" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="CandidatesPanel"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="OnCancel" Margin="0,0,8,0"/>
            <Button x:Name="AdoptButton" Content="Adopt Selected" Click="OnAdopt"
                    Background="#3B7D3B" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
